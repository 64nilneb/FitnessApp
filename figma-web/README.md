# Figma Make Web Template (Reference)

This folder contains the original Figma Make export (Vite + React + Tailwind). Use it as a reference when migrating screens to React Native in the root `app/` directory.

To run the original web preview:

```bash
cd figma-web
npm install
npm run dev
```
